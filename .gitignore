node_modules
dist/
coverage/
artifacts/
release/
.stryker-tmp/
*.log
.DS_Store
.env
.env.*
!.env.example
agentops.env

# Local-only planning material
brief.pipeline.md
brief.host.md
brief.growth.md
brief.growth.plan.md
gtm/

# Private keys, certificates, and machine-local credentials
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
credentials*.json
service-account*.json
packaging/sea/out/
packaging/sea/cache/
packaging/sea/launcher/
packaging/linux/build/

# Maven build output of the Java verifier
verifiers/spring/target/
verifiers/spring/test-output/

# Cargo build output of the Rust verifier; the crate is a library, so its
# lockfile is the consumer's, not this repository's.
verifiers/rust/target/
verifiers/rust/Cargo.lock

# .NET build output of the .NET verifier
verifiers/dotnet/**/bin/
verifiers/dotnet/**/obj/

# Govern: the binary `go build ./cmd/govern` leaves beside the module, and a
# run's dotenv report written where the pipeline asked
govern/govern
govern/*.env
