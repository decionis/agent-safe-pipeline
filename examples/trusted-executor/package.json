{
  "name": "@decionis/agent-safe-example-trusted-executor",
  "version": "0.1.0",
  "private": true,
  "description": "The trusted executor from @decionis/agentsafe, proved offline over real HTTP against the loopback doubles, and the template an adopter starts from.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node ../../scripts/CleanBuildOutput.mjs && tsc -p tsconfig.json",
    "demo": "pnpm --filter @decionis/agent-safe-pipeline build && pnpm --filter @decionis/agentsafe build && pnpm build && node dist/Index.js",
    "serve": "node dist/Serve.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@decionis/agent-safe-pipeline": "workspace:*",
    "@decionis/agentsafe": "workspace:*",
    "jose": "^6.2.10"
  }
}
