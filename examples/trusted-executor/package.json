{
  "name": "@decionis/agent-safe-example-trusted-executor",
  "version": "0.1.0",
  "private": true,
  "description": "The execution boundary as one deployable process: a trusted executor service that receives proposals over HTTP, asks Decionis, and executes once on a claimed single-use grant.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node ../../scripts/CleanBuildOutput.mjs && tsc -p tsconfig.json",
    "demo": "pnpm --filter @decionis/agent-safe-pipeline build && pnpm build && node dist/Index.js",
    "serve": "node dist/Serve.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@decionis/agent-safe-pipeline": "workspace:*",
    "zod": "^4.5.4"
  }
}
