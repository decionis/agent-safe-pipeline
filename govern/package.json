{
  "name": "@decionis/govern-contract",
  "version": "2.0.0",
  "description": "Govern's test against the pipeline's loopback Decionis: the Go binary speaking the execution contract to another implementation of it.",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/decionis/agent-safe-pipeline.git",
    "directory": "govern"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@decionis/agent-safe-pipeline": "workspace:*"
  }
}
